#pragma once

#include <memory>
#include <UUID.h>

DEF_UUID_TYPE(DeviceUtils, ConstantsBuffer, GetConstantsBuffer)
DEF_UUID_TYPE(DeviceUtils, RenderToTexturePass, GetRenderToTexturePass)
DEF_UUID_TYPE(DeviceUtils, SwapChainPass, GetSwapChainPass)
DEF_UUID_TYPE(DeviceUtils, RenderToTexture, GetRenderToTexture)